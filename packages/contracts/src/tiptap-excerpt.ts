/** Backend notifications / email-style excerpts (aligned with API default truncation). */
export const TIPTAP_NOTIFICATION_EXCERPT_MAX = 220;

/** Issue activity payloads (`description_changed` excerpt). */
export const TIPTAP_ISSUE_DESCRIPTION_ACTIVITY_EXCERPT_MAX = 280;

/** Composer / quoted bubble preview (single-line UI). */
export const TIPTAP_QUOTED_MESSAGE_UI_EXCERPT_MAX = 160;

/** Message card inline quoted snippet. */
export const TIPTAP_MESSAGE_BOX_QUOTE_EXCERPT_MAX = 220;
